#include <vector>
#include <stdio.h>

int main()
{
    int n, m, a, b;
    scanf("%d %d", &n, &m);

    std::vector<bool> visit(n, false);
    std::vector<std::vector<int>> adj(n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d %d", &a, &b);
        adj[1].push_back(3);
    }

    return 0;
}
