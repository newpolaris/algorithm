#include <cstdio>

// MST simple version, based on '1197.cubelover.py'

int n, m, a, b, c;
int main() {
    scanf("%d %d", &n, &m);
    for (int i = 0; i < m; i++) {
        scanf("%d %d %d", &a, &b, &c);
    }
    return 0;
}
