#include <iostream>
#include <vector>

using namespace std; 

typedef long long ll;
ll mod = 1e9+7;
ll comb[2001];

int main() {
#ifdef _DEBUG
	freopen("712d.in", "r", stdin);
#endif

	int a, b, k, t;
	cin >> a >> b >> k >> t;
	
	comb[0] = 0, comb[1] = 0;
	for (int i = 0; i < t; i++)
	{
		comb[
	}

	return 0;
}
