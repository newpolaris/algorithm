#include <iostream>
#include <string.h>
#include <stdio.h>
#include <vector>
#include <map>
#include <algorithm>
#include <queue>

using namespace std;
typedef vector<int> vi;

int main()
{
	freopen("C.in", "r", stdin);

	int N;
	vi L(N);
	for (auto &e : L) {
		scanf("%d", &e);
		e--;
	}

	vi in(N);
	for (int i = 0; i < N; i++)
		in[i] = i;

	for (int i = 0; i < N; i++)
		in

	return 0;
}

