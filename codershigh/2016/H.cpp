#include <iostream>
#include <string.h>
#include <stdio.h>
#include <vector>
#include <map>
#include <algorithm>
#include <queue>

using namespace std;

typedef long long ll;
typedef vector<ll> vl;

int main()
{
	ll N, M;
	scanf("%lld%lld", &N, &M);

	vl L(M);
	for (auto& l : L)
		scanf("%lld", &l);

	

	return 0;
}

